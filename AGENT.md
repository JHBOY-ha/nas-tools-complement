# 项目方案

## 字幕设置与任务资源限制

- `web/templates/setting/subtitle.html` 只负责字幕服务配置；配置弹窗内仅保留并突出 OpenSubtitles API Consumers 快捷按钮，不另设重复的官网入口，首页卡片只打开配置。
- `web/templates/setting/subtitle_task_settings.html` 为独立任务设置页，经登录保护的 `/subtitle_task_settings` 路由加载，兼容现有 `navmenu` 的 POST 加载及浏览器历史恢复。
- `web/templates/rename/medialibrary.html` 在字幕库工具栏提供任务设置入口；“任务设置”与“任务中心”共用尺寸规则。
- 文件管理页“字幕任务”通过 `mediafile-task-action` 沿用默认 Tabler 按钮尺寸，与“转移目录”一致；局部覆盖任务中心动态注入的最小高度，保留任务计数与点击行为。
- 子页面保留导航栏“媒体整理 → 字幕库”选中状态，提供返回字幕库和共享任务中心入口。
- 资源限制按上传队列与空间、进程与任务预算、检测与记录分组；每个字段旁以基础设置同款 `form-help` 问号和 Bootstrap tooltip 悬停/聚焦展示说明，保留隐藏的输入框辅助说明；字幕库状态说明只统一问号外观，仍打开原有弹窗，默认隐藏已读取状态，保留保存和错误反馈。配置键、安全范围和后端执行语义保持不变。
- `web/static/js/subtitle-task-settings.js` 复用 `SubtitleTasks.request`，管理读取、保存、字段校验及失败反馈。初次读取失败禁止保存，请求期间禁止重复操作，离开页面后旧请求不得更新新页面。
- `/subtitle/tasks/settings` 继续作为唯一策略读写接口，管理员权限与新任务配置快照规则由现有后端负责。
- 字幕上传 multipart 解码缓冲与普通字段预算分别限制，解码缓冲必须大于 Werkzeug 2.1 的 64 KiB 读取块；字段总量仍限制为 64 KiB。字幕检测仅在目录完整枚举后发布否定覆盖，部分目录只发布已确认的中文字幕肯定状态。

## 验证原则

模板渲染与字段迁移完整性检查、JS 语法检查、相关字幕任务回归测试；浏览器检查页面跳转、配置和官网入口独立响应、窄屏布局、读取/保存失败与校验。预览使用模拟接口时不宣称真实后台联调通过。

## OpenSubtitles 安全与字幕状态定向查询

- OpenSubtitles API 登录、搜索、用户信息和下载授权只允许 HTTPS，启用证书校验且禁止自动重定向；登录返回的 `base_url` 必须规范化且包含有效主机，否则清除令牌并恢复官方 API 根地址。
- 临时字幕链接只接受 HTTPS，以 64 KiB 小块流式读取并在 20 MiB 硬上限内才进行编码和内容验证。所有路径关闭响应；仅连接/读取异常和 5xx 最多重试一次，4xx、超限、HTML/JSON 或无效字幕不重试。
- `SUBTITLE_AUDIT_STATE.RESULT` 中的每条 `checked_at` 与该行 `CONFIRMED_AT` 一致；旧行聚合时用自身 `CONFIRMED_AT` 回填。媒体卡片不使用分类批次时间伪装单条复检时间。
- 字幕库默认列表先完成业务筛选和分页，再按当前页媒体路径分块查询 `SUBTITLE_MEDIA_STATUS` 和 `SUBTITLE_AUDIT_STATE`；字幕筛选或排序仅将查询扩展到已通过媒体类型、分类和关键字的候选路径，始终不访问 NAS 或运行 ffprobe。
- 任务列表用一次全局等待队列查询，严格按 `PRIORITY DESC, CREATED_AT ASC, ID ASC` 预计算位置；单任务详情仍可按需查询。

## 字幕库低 I/O 与文本对齐方案

- 字幕库列表、翻页、筛选、排序和普通刷新只读取媒体同步表、转移历史和 `SUBTITLE_MEDIA_STATUS`，不得访问媒体路径、枚举 NAS 目录或启动 ffprobe。没有快照且媒体服务器流信息不明确时必须显示“未检测”。
- 外挂字幕检测负责写入已覆盖媒体的正/负快照；上传和二次处理仅复检目标媒体，且复检继承取消、超时与重操作并发限制。已发布后取消或超时时，仅根据发布事实写 SQLite 快照，不继续访问 NAS。服务启动、升级和页面刷新不触发全库扫描。
- 封面服务失败时按项目 ID 定向查询同步记录与转移历史，并缓存成功和失败回退，禁止为单张封面加载全量媒体数据。
- 文本对齐只使用视频内嵌文本字幕：n-gram 候选索引、单调锚点链、MAD 异常值剔除、稳健偏移/轻微线性漂移/确认分段模型及写入前时间轴校验。分段控制点使用连续窗口中位值，模型应用后再校验锚点 P95 残差；低置信度或校验失败保留原字幕。
- 内嵌参考字幕缓存以媒体大小、mtime、流索引、缓存版本和 ffmpeg 版本为指纹，保留 30 天，容量不超过字幕暂存额度 10%且最多 256 MiB；仅完整抽取结果原子写入缓存。
- LLM 跨语言模式只翻译均匀分布的高信息量样本，首次匹配不足时才使用剩余批次，不翻译完整参考字幕。
- 本方案排除 TTS、ASR、VAD、Whisper、音频切片、模型下载/管理、硬件探测和音频强制对齐。端侧模型必须在独立后续分支设计，本分支不预埋代码、配置、接口、字段或依赖。

## 剧集名称与季集识别方案（已实施）

- 可追踪的使用说明见 `docs/meta-season-episode-recognition.md`；实现入口为 `app/media/meta/metainfo.py`、`app/media/media.py` 和 `app/filetransfer.py`。
- 暂缓裸四位数字识别及其专属 TMDB 映射扩展，等待实际失败资源样本；保留已有明确集号格式兼容。
- NCOP、ED、PV、SP 不再硬编码拦截；用户通过文件名转移忽略词 `media.ignored_files` 控制整理过滤，未配置时正常识别。独立名称识别不受转移忽略词影响，小数集保护保留。不自动修改用户配置。
- 小数集不得取整、区间化或默认 S00；只有可靠匹配到 TMDB 具体集时使用正式编号，否则保留文件并跳过整理。允许为此在整理入口增加必要确认和拦截，不重构现有映射机制。
- 其余修复保留年份路由、Season/Episode 前后置、1x03 和明确季集描述；验证片名完整性、编码兼容、备注隔离与跳过路径。
- `media.fractional_episode_mappings` 可配置小数集的作品 ID、原始编号、TMDB 正式季集及单集标题或播出日期证据；只有 TMDB 单集详情验证证据后才整理。识别与转移回归保存在 `tests/test_meta_recognition_plan.py`。

- 审查修复：小数集保护覆盖 `EP01.5`、`S01E01.5` 和 `S01EP01.5`，保留发布季以匹配配置；正式映射与下载任务冲突时保留源文件并跳过，不允许上下文重写正式季集。英文 Season/Episode 仅在紧跟有效编号时作为标记，否则保留片名。

- 数字电影：详情查询接受 `530915-1917` 形式的网页末段并提取数值 ID；显式电影类型或已绑定电影详情下，`1917.mkv` 作为片名解析，无集号。未知类型的纯数字剧集命名保持兼容。

- 季集审查修复：RSS 根据文件解析出的季号检查冲突，兼容中文、罗马数字和 `2x03`，不再只检查英文标记。手动识别传入的空 `EpisodeFormat` 不阻断正式季集映射及 TMDB 校验；有实际手动规则时继续尊重用户指定。明确 `S01E01-E03` / `E01-03` 等区间允许三集以上，裸数字推断仍保留保护上限。小数集边界与剪辑版本的后续实现见下节。

## 小数集批次确认与剪辑版命名

- 小数集在整数文件识别后确认，保留原编号、发布季和 A/B 后缀；只用配置验证、唯一标题或 TMDB 明确发布编号证据绑定正式整数季集。全部季列表完整才允许自动唯一匹配，批内缓存查询，失败下批重试；不按目录继承作品。
- 下载上下文使用稳定任务键复用身份；新增上下文标明正式 TMDB 编号，旧语义冲突时保守跳过。未确认文件保源、失败返回、不入成功黑名单；小数集目标碰撞在任何转移前检测，同一 inode 可重试。
- 新增独立 cut 命名字段，保持 edition 的来源/技术含义；LLM 技术字段清除剪辑词，HDR10+ 保留加号。只更新占位符帮助，不修改用户模板、忽略词、已有媒体文件或数据库结构。
- `tests/test_fractional_versions.py` 使用 TMDB 记录的离线样本与临时文件验证边界、映射、缓存、冲突、版本命名；不能将此描述为实际 NAS/Jellyfin 联调。

- 剪辑版补充：支持紧贴年份之前的完整剪辑短语（如 `Rambo.Extended.Cut.2008`），要求前有有效片名、年份后有资源标记；不对任意 Cut/Extended 单词作全局删除。保留 `The Directors Cut`、`Uncut Gems` 等反例测试。

- 基础设置的电影重命名帮助同步说明 cut 的五类标签、年份前后及括号识别条件、多标签去重和未识别留空；不修改已保存的命名格式。

- 审查修复：小数集保护排除完整点分日期及整数集号后的位深/分辨率标签；四位裸数字范围终点不作明确集数，保留 E01-03。目标预检遇到源文件消失仅使冲突组失败；已确认小数集继续按正式季集跨目录去重，无历史的同 inode 目标仍可补写记录。
- 新整季下载上下文另外保存 scope=season_pack 与 release_seasons；只允许来自该发布季且已经独立确认的 S00 小数特别篇，不放宽选集、旧上下文或其他发布季。剪辑别名与旧名称清理子集集中维护，保持各自识别边界；特典过滤继续由用户忽略词决定。
